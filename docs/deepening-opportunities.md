# Architecture Deepening Opportunities

_Generated by `/improve-codebase-architecture` on 2026-05-05_

> **Status 2026-09-18 (rodada pós-importadores):**
> - **Bug de transposição corrompendo letras — RESOLVIDO**: `transposeCifra` aplicava regex de acorde em toda linha não-tab ("Deus"→"Eeus" ao transpor). Agora transpõe só colchetes ChordPro, linhas só-de-acorde e trechos rotulados ("INTRO: ..."). Regressão coberta em `chord-transposer.test.ts`.
> - **Pipeline de importação duplicado — RESOLVIDO**: `src/lib/import-pipeline.ts` (`normalizeImportedSong` + `saveImportedSong`) é o caminho único das rotas `/api/import-song` e `/api/import-musicasparamissa`; detecção de tom unificada (metadado → "Tom:" → `detectKeyFromCifra`).
> - **Vocabulário de acordes divergente — PARCIAL**: `src/utils/chord-pattern.ts` (fonte única, com `maj` antes de `m` — shadowing "Cmaj7"→"Cm" corrigido) adotado por chordpro-converter, key-detector e chord-transposer. PENDENTE: `components/chords/chord-parser.ts`, `ChordViewer.tsx`, `CifraPreview.tsx` ainda têm regexes próprias (semântica de display; migrar junto com o CifraDocument abaixo).
> - **Novos candidatos registrados** (ver seção "Candidatos 2026-09-18" ao final): CifraDocument único, motor de ritmo duplicado (RhythmPlayer×DrumPad×página de música), persistência de tom ×3, setlist evento×template, "lista de estudo" como null-sentinela, adoção do apiFetch, update dinâmico de campos, fontes canônicas de labels/search.
> - **#4 e #5 continuam como estavam** (#4 piorou: bpm/volume/drum_pattern_id agora têm 3 writers distintos).

> **Status 2026-07-15 (limpeza + refactors chave):**
> - **#1 Tag serialization — RESOLVIDO**: `tag-utils.ts` (`parseTags`/`stringifyTags`) aplicado em todos os módulos db, rotas e páginas.
> - **#2 Type drift — RESOLVIDO**: `types/database.ts` é a fonte única; módulos `*-db.ts` importam de lá; aliases `*Insert/*Update` mortos removidos; `Evento.data` agora `string | null` (verdade do banco).
> - **#3 ChordViewer parsing duplicado — RESOLVIDO**: detectores (`isTabLine`, `isStrummingLine`) exportados de `chord-transposer.ts`; ChordViewer virou renderer puro (caminho tonal morto removido junto com `@tonaljs/tonal`).
> - **#4 musicasDb + performance settings — PENDENTE** (bpm/volume/drum_pattern_id ainda em `musicas`).
> - **#5 API routes/service layer — PARCIAL**: `api-helpers.ts` (`jsonError`, `parseId`, áudio) eliminou ~800 LOC de boilerplate e padronizou erros; rotas de templates/drum-patterns voltaram a usar módulos db (drift de ORDER BY corrigido); importar/clone agora transacionais via `setlistsDb.copyFromTemplate/copyFromEvento`. Service layer completa continua como follow-up.

---

## 1. Tag Serialization Spread Across DB Modules

**Files:** `src/lib/musicas-db.ts`, `src/lib/eventos-db.ts`, `src/lib/setlists-db.ts`, `src/lib/practice-sessions-db.ts`

**Problem:** Every DB module independently handles tag serialization. `JSON.parse(row.tags || '[]')` appears at least 12 times across the codebase. If the schema changes (e.g., `null` vs `'[]'` handling, or a different serialization format), you'd need to update all of them. This is **low locality** — the knowledge of "how to serialize tags" is spread everywhere instead of living in one place.

**Solution:** Extract tag serialization into a dedicated module (e.g., `tag-serializer.ts`) with `serialize()` and `deserialize()` functions. The DB modules become clients of this module.

**Benefits:**
- **Locality**: One place to change if tag format evolves
- **Leverage**: Any future tag-related logic (validation, merging, filtering) lives here
- **Tests**: Easy to test serialization edge cases in isolation

---

## 2. Type Drift Between `types/database.ts` and DB Modules

**Files:** `src/types/database.ts` vs `src/lib/musicas-db.ts`, `src/lib/eventos-db.ts`, `src/lib/setlists-db.ts`

**Problem:** `Musica` has **two different shapes** depending on which file you look at:
- `types/database.ts`: `tom_original`, `cifra`, `tags`, no `observacao`, no `drum_pattern_id`
- `musicas-db.ts`: Has `observacao`, `audio_url`, `groove`, `drum_pattern_id`, `bpm`, `volume`

The `types/database.ts` version is incomplete — it's not the source of truth. Every API route and component using `types/database.ts` is working with a stale view of the actual domain.

**Solution:** Make `musicas-db.ts` (and similar) **the** source of truth for domain types, and have `types/database.ts` import from them. Or consolidate into a single `domain/` module.

**Benefits:**
- **No more drift**: One definition, not three
- **Locality**: Bugs from type mismatches concentrate in one place
- **Leverage**: Refactoring the domain type is one edit, not four

---

## 3. ChordViewer Owns Duplicate Transposition Logic

**Files:** `src/utils/chord-transposer.ts` vs `src/components/chords/ChordViewer.tsx`

**Problem:** `ChordViewer.tsx` contains its own implementations of:
- `isChordProFormat()`
- `isTabLine()`
- `isStrummingLine()`

These are duplicated from `chord-transposer.ts`. The transposition logic itself lives in `transposeLine()` / `transposeCifra()`, but the **parsing** logic (detecting what kind of line this is) is duplicated. This means bugs in tablature detection could be fixed in one place but remain in another.

**Solution:** Move all chord/line detection and transposition logic into a single **Chordpro parser module** behind an interface. Both `ChordViewer` and `CifraViewer` become thin clients of this module.

**Benefits:**
- **Locality**: Bug fixes and enhancements to chord/tab detection happen once
- **Leverage**: A test of the parser module exercises the full logic surface
- **Deepening**: The module earns its keep — it contains real knowledge about how to parse cifra formats

---

## 4. `musicasDb` is Absorbing Wider Concerns

**Files:** `src/lib/musicas-db.ts`

**Problem:** The `Musica` data access module has organically grown to include `drum_pattern_id`, `bpm`, `volume` — which are really **performance settings**, not core song metadata. These concerns are orthogonal to "a song exists in the catalog."

Deletion test: if you deleted `musicas-db.ts` but needed to change how `bpm` is stored or validated, would the complexity disappear or would it reappear across call sites? It would **reappear** — meaning the module isn't actually hiding complexity, it's just moving it.

**Solution:** Consider whether `bpm`, `volume`, and `drum_pattern_id` belong in a separate **PerformanceSettings** module (or attached to the EventoMusica join, since those settings might be per-event, not per-song).

**Benefits:**
- **Locality**: Performance settings bugs fix in one place
- **Leverage**: If settings vary per-event (transposition does), this model is more accurate
- **Tests**: Smaller surface to test per-song vs per-event behavior

---

## 5. API Routes Are Thin Pass-Throughs

**Files:** `src/app/api/musicas/[id]/route.ts` and 30+ others

**Problem:** API routes do request parsing → call DB module → return JSON. There's no **service layer** to hold business logic. This means:
- Validation is ad-hoc and duplicated
- Composite operations (e.g., "create evento from template + copy musicas + set initial status") are either duplicated or handled by one route that shouldn't own that knowledge

**Solution:** Introduce a service layer module between API routes and DB modules. API routes become thin adapters.

**Benefits:**
- **Locality**: Business rules have a home
- **Leverage**: Common validation (e.g., "can't add a music to a canceled event") lives once
- **Tests**: Service layer is easy to unit test without HTTP

---

## Candidatos 2026-09-18 (registrados, não executados)

1. **CifraDocument único** — parse uma vez (linhas classificadas + acordes) e derive render/transposição/conversão do mesmo modelo. Unificaria `chord-parser.ts`, `chordpro-converter.ts`, `chord-transposer.ts`, `ChordViewer`, `CifraPreview` (que ainda renderiza por alinhamento de espaços, técnica abandonada pelo ChordViewer) e as 3 definições divergentes de "linha de acorde" (`isChordLine`, `isSpacedChordLine`, `isChordOnlyLine`). Linhas rotuladas ("INTRO: Eb G#") hoje têm 3 classificações diferentes.
2. **Motor de ritmo duplicado** — `RhythmPlayer.tsx` e `DrumPad.tsx` implementam o mesmo engine (fetch patterns, resolução db/preset, Sampler+Limiter, scheduler de 16ths idêntico); `musicas/[id]/page.tsx` tem uma terceira implementação com `Tone.Sequence`. `PRESET_GROOVES` (130 linhas de dados) mora num componente. Extrair módulo de engine + hooks de UI finos.
3. **Persistência de tom ×3** — bloco debounce+PUT quase idêntico em setlist, cifra e ensaio; `useDrumPadSettings` é o precedente pronto (hook `useTomPersistence`).
4. **Setlist evento×template** — universos paralelos (`setlists-db` × `template-musicas-db`, rotas espelhadas com contratos divergentes: delete por path vs query param, 200 vs 201); `utils/setlist-api.ts` já esconde a dualidade no cliente — o servidor continua dobrado.
5. **"Lista de estudo" como null-sentinela** — `data === null` codifica o conceito em 5+ lugares (JSX, payload, API); tornar o tipo explícito e centralizar a regra de nulificação.
6. **`apiFetch` adotado pela metade** — ~15 arquivos repetem fetch+ok+json+erro inline; considerar hook de data-fetching.
7. **Update dinâmico de campos** — whitelist campo-a-campo na rota + if-blocks no db (adicionar campo a Música exige tocar 4 camadas); helper campo→coluna confiando no zod como fronteira.
8. **Fontes canônicas ignoradas** — `STATUS_LABELS` locais apesar de `EVENTO_STATUS_LABELS` em types/database.ts; filtro de músicas no cliente enquanto `musicasDb.search` existe no servidor (decidir qual caminho é o oficial).
9. **Scraper cifraclub: separar lifecycle de browser de extração** — extrair funções puras de extração (recebem HTML) como já existe em musicasparamissa.ts, destravando testes sem Playwright.
10. **URL do Cifra Club espalhada** — `parseCifraClubUrl` na rota + parsing inline 2× em musicas/new/page.tsx + reconstrução em VersionSelector; mover para o scraper.

---

## Glossary

- **Module** — anything with an interface and an implementation (function, class, package, slice)
- **Interface** — everything a caller must know to use the module: types, invariants, error modes, ordering, config
- **Depth** — leverage at the interface: a lot of behaviour behind a small interface
- **Seam** — where an interface lives; a place behaviour can be altered without editing in place
- **Leverage** — what callers get from depth
- **Locality** — what maintainers get from depth: change, bugs, knowledge concentrated in one place